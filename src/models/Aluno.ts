// import type Categoria from "./Categoria";

export default interface Postagem {
  id: number;
  nome: string;
  telefone: string;
  altura: number;
  peso: number;
  // categoria: Categoria | null;
}
